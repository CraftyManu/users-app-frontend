import { colors } from "./colors";

export const shadows = {

    card:

        "0 10px 30px rgba(0,0,0,.18)",

    glow:

        `0 0 20px ${colors.primary}`,

    root:

        `0 0 14px ${colors.role.ROOT}`,

    admin:

        `0 0 14px ${colors.role.ADMIN}`,

    user:

        `0 0 14px ${colors.role.USER}`,

    guest:

        `0 0 14px ${colors.role.GUEST}`,

};